<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li><%= @post.title %></li>
    <li><%= @post.body %></li>
    <% if user_signed_in? %>
      <%= post.user.username %>
    <% end %>
  <% end %>
</ul>

<% if user_signed_in? %>
  <%= link_to 'New Post', new_post_path %>
<% end %>
